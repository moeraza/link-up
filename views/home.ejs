<% include partials/header %>
<div class="container">
    <div class="row justify-content-center">
        <h1 class="text-center">This is the home page!</h1>
    </div>
    <div class="row justify-content-center">
                <% if(currentUser){ %>
            <a href="/<%= currentUser.username%>">
                <button type="button" class="btn btn-outline-success"><%= currentUser.username%>'s LinkUp Page</button>
            </a>
        <% } %>
    </div>
</div>




<% include partials/footer %>
